# Build prerequisites (must be installed first — some packages below use legacy setup.py)
setuptools>=70.0
wheel

# ── Inference / runtime ───────────────────────────────────────────────────────
# NOTE: install llama-cpp-python SEPARATELY with CUDA for GPU inference:
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu122 --upgrade
# The version pin below is the CPU fallback.
llama-cpp-python>=0.3.20

# Speech-to-text (offline)
openai-whisper>=20240930
transformers>=4.41.0
torch>=2.2.0
imageio-ffmpeg>=0.4.9

# Web API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Audio I/O (mic recording + TTS)
pyaudio>=0.2.14
pyttsx3>=2.90

# Vision (image preprocessing for multimodal Gemma 4)
opencv-python>=4.9.0
Pillow>=10.2.0

# Data + evaluation
datasets>=2.18.0
geopy>=2.4.1
scikit-learn>=1.4.2
huggingface_hub>=0.22.2

# Tests
pytest>=8.0.0

# ── Training-only (Kaggle/Colab — do NOT install locally on Windows) ──────────
# unsloth and trl are needed only inside the Kaggle notebook.
# They require Linux + CUDA-built torch and will break a local Windows install.
# See: finetune/kaggle_unsloth_train.ipynb
